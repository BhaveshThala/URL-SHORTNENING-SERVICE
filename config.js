const config = {
    db: {
      /* don't expose password or any sensitive info, do only for testing */
      host: "your host name",
      user: "user name",
      password: "password",
      database: "database name",
      connectTimeout: 60000
    }
  };
  export default config;